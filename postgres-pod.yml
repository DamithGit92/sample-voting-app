apiVersion: v1
kind: Pod
metadata:
  name: postgres-pod
  labels:
    name: postgres-pod
    app: demo-voting-app

spec:
  containers:
  - name: postgres
    image: postgres:12
    ports:
    - containerPort: 5432
    restart: always
    #command: [ "/bin/bash", "-ce", "tail -f /dev/null" ]